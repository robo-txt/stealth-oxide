services:
  stealth-oxide:
    build:
      context: .
    cap_add:
      # Chromium's Linux sandbox creates its own PID/network namespaces.
      - SYS_ADMIN
    init: true
    shm_size: 2gb
    volumes:
      - ./container/fonts:/usr/local/share/fonts/windows:ro
